import SwiftUI

/// Temporary blank root. Replaced by the coordinator-driven shell in Sprint 1.
struct RootView: View {
    var body: some View {
        Color.clear
            .ignoresSafeArea()
    }
}

#Preview {
    RootView()
}
